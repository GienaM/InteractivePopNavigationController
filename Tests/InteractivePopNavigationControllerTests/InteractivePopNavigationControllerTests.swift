import XCTest
@testable import InteractivePopNavigationController

final class InteractivePopNavigationControllerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(InteractivePopNavigationController().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
